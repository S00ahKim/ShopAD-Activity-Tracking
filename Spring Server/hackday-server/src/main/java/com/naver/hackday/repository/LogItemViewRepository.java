package com.naver.hackday.repository;

import com.naver.hackday.domain.LogItemView;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogItemViewRepository extends JpaRepository<LogItemView, Integer> {
}
