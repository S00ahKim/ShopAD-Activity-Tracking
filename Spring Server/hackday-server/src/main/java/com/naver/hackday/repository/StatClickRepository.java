package com.naver.hackday.repository;

import com.naver.hackday.domain.StatClick;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StatClickRepository extends JpaRepository<StatClick, Integer> {
}
